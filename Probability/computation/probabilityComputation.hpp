#ifndef probabilityComputation
#define probabilityComputation

#include <iostream>
#include <vector>

using namespace std;

float findSumOfList(vector<float> input);

float findProbabilityOfListFromList(vector<float> input, vector<float> list);


#endif